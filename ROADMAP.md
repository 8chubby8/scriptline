# Roadmap

Planned work, roughly in order. Move items to CHANGELOG.md once they're done.

## Next

- Try the prototype for real and tune by feel: tier fade-in points, zoom speed, row spacing, colours.
- Folder sync (Chrome/Edge desktop): one file per entry, plus a pictures folder.
- Export / import as files, so data can move between devices and be backed up.
- Put the site on GitHub Pages.

## Later

- Sharing a single entry together with its pictures (leading idea: a "Share" bundle file).
- Swap the hand-written pan/zoom for a small helper library, if momentum and trackpad feel need it.
- Possibly more than two tracks.
- Possibly: highlight an entry's family and dim the rest, if busy stretches get hard to follow.
